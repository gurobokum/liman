---
title: LLMNode
description: LLMNode is a node that use LLM to generate text.
---

## Specification

```yaml
kind: LLMNode
name: string
prompts: LanguageBundle
to?: (string | Edge)[]
tools?: ToolNode[]
structured_output?: Schema

# legacy
nodes?: (Node | Edge<Node>)[]
```

import { TypeTable } from "fumadocs-ui/components/type-table";

<TypeTable
  type={{
    kind: {
      description: "The kind of the node, must be 'LLMNode'.",
      required: true,
      type: "LLMNode",
    },
    name: {
      type: "string",
      required: true,
    },
    prompts: {
      type: "LanguageBundle",
      typeDescriptionLink: "/docs/specification/internal_types#languagebundle",
      description:
        "The prompts for the LLMNode, including system and user prompts.",
      required: true,
    },
    tools: {
      type: "ToolNode[]",
      typeDescriptionLink: "/docs/specification/tool_node",
      description: "The tools that the LLMNode can use.",
      required: false,
    },
    to: {
      type: "(string | Edge)[]",
      typeDescriptionLink: "/docs/specification/internal_types#edge",
      description:
        "Canonical routing field. Edges to any node kind with explicit 'Kind/name' refs. Cannot be mixed with 'nodes'.",
      required: false,
    },
    nodes: {
      type: "(Node | Edge<Node>)[]",
      typeDescriptionLink: "/docs/specification/node",
      description:
        "Alternative routing form scoped to Node targets. Cannot be mixed with 'to'.",
      required: false,
    },
    structured_output: {
      type: "Schema",
      description:
        "Constrain the LLM response to a typed structure. Cannot be used together with tools.",
      required: false,
    },
  }}
/>

## Structured Output

When `structured_output` is set, the node returns a typed result instead of a plain text message.
Use a simplified inline schema - keys are field names, values are types.
A `?` suffix on a key marks the field as optional.

```yaml
kind: LLMNode
name: BookExtractor
prompts:
  en:
    system: Extract book information from the text.
structured_output:
  title: str
  author: str
  year: int
  genre: str
  summary?: str
```

Supported scalar types: `str`, `int`, `float`, `bool`.

In the short form, add a `#` comment on the line before a field to set its description.
The description is injected into the system prompt and the JSON schema sent to the LLM.
Multi-line comments are joined with a space into a single description string.

```yaml
structured_output:
  # The main title of the book as it appears on the cover
  title: str
  # The full name of the primary author, including first and last name
  author: str
  # The year the book was first published;
  # use 0 when the year cannot be determined from the text
  year: int
```

For more control, use the extended form. Each field becomes a mapping with `type`, `description`, and `optional` keys.
Comments are not read in this form - use `description` instead.

```yaml
structured_output:
  title:
    type: str
    description: The main title of the book as it appears on the cover
  year:
    type: int
    description: The year the book was first published; use 0 when unknown
    optional: true
```

`description` also accepts a localized value to match the prompt language:

```yaml
structured_output:
  title:
    type: str
    description:
      en: The main title of the book as it appears on the cover
      ru: Название книги, как оно указано на обложке
```

For arrays, wrap the type in a list:

```yaml
structured_output:
  tags: [str]
  scores: [int]
```

For nested objects, use a nested mapping:

```yaml
structured_output:
  title: str
  publisher:
    name: str
    country: str
```

<Cards>
  <Card
    href="/docs/specification/internal_types#languagebundle"
    title="LanguageBundle"
  >
    Block with text supported localization.
  </Card>
  <Card href="/docs/specification/internal_types#edge" title="Edge">
    An Edge allows you to define a connection between nodes using a DSL
    condition.
  </Card>
</Cards>
