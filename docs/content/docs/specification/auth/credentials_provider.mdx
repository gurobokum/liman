---
title: Credentials Provider
description: Defines how to obtain credentials for a ServiceAccount
---

A CredentialsProvider defines how to obtain credentials for a ServiceAccount. It can fetch credentials from various sources such as environment variables, files, or secret management systems.

It accepts a service account and should return credentials in dict format.

```python
{
    "type": "bearer",
    "data": "string"
}
```

or a more complex structure based on the provider type.

```python
{
    "type": "aws_s3",
    "data": {
        "access_key_id": "string",
        "secret_access_key": "string"
    }
}
```

## Specification

```yaml
kind: CredentialsProvider
name: string
type: bearer | basic | aws | gcp | azure | custom
func: string
```

import { TypeTable } from "fumadocs-ui/components/type-table";

<TypeTable
  type={{
    kind: {
      description:
        "The kind of the specification, must be 'CredentialProvider'.",
      required: true,
      type: "CredentialsProvider",
    },
    name: {
      type: "string",
      required: true,
    },
    type: {
      type: "string",
      description:
        "The type of credentials to be provided. Supported types include 'bearer', 'basic', 'aws', 'gcp', 'azure', and 'custom'.",
      required: true,
    },
    func: {
      type: "string",
      description: "The function used to fetch or generate the credentials",
      required: true,
    },
  }}
/>

## Example

```yaml title="credentials_provider_bearer.yaml"
kind: CredentialsProvider
name: get_bearer_token
type: bearer
func: main.get_bearer_token
```
