---
title: instrumentor
---

<PyAttribute
  name={"logger"}
  type={null}
  value={"logging.getLogger(__name__)"}
/>

<Tabs items={["Class","Functions"]}>

<Tab value={"Class"}>

<Cards >

<Card
  title={"LimanInstrumentor"}
  href={"/docs/python/liman_finops/instrumentor/LimanInstrumentor"}
/>

</Cards>

</Tab>
<Tab value={"Functions"}>

<PyFunction name={"configure_instrumentor"} type={"configure_instrumentor(span_exporter=None, /, metric_exporter=None, *, console=False, service_name='liman') -> LimanInstrumentor"}>

Configure the Liman instrumentor.

Args:
console: Enable console exporters for debugging
span_exporter: Custom span exporter (e.g., Jaeger, OTLP, etc.)
metric_exporter: Custom metric exporter
service_name: Name of the service for telemetry data

<PySourceCode >

```python
def configure_instrumentor(
    span_exporter: SpanExporter | None = None,
    metric_exporter: MetricExporter | None = None,
    *,
    console: bool = False,
    service_name: str = "liman",
) -> "LimanInstrumentor":
    """
    Configure the Liman instrumentor.

    Args:
        console: Enable console exporters for debugging
        span_exporter: Custom span exporter (e.g., Jaeger, OTLP, etc.)
        metric_exporter: Custom metric exporter
        service_name: Name of the service for telemetry data
    """

    resource = Resource.create({ResourceAttributes.SERVICE_NAME: service_name})
    tracer_provider = TracerProvider(resource=resource)
    meter_provider = None

    if console:
        processor = BatchSpanProcessor(ConsoleSpanExporter())
        tracer_provider.add_span_processor(processor)

        reader = PeriodicExportingMetricReader(ConsoleMetricExporter())
        meter_provider = MeterProvider(resource=resource, metric_readers=[reader])

    if span_exporter:
        processor = BatchSpanProcessor(span_exporter)
        tracer_provider.add_span_processor(processor)

    if metric_exporter:
        reader = PeriodicExportingMetricReader(metric_exporter)
        meter_provider = MeterProvider(resource=resource, metric_readers=[reader])

    instrumentor = LimanInstrumentor()
    instrumentor.instrument(
        tracer_provider=tracer_provider, meter_provider=meter_provider
    )

    return instrumentor
```

</PySourceCode>

<div >

<PyParameter
  name={"span_exporter"}
  type={"SpanExporter | None"}
  value={"None"}
/>
<PyParameter
  name={"metric_exporter"}
  type={"MetricExporter | None"}
  value={"None"}
/>
<PyParameter name={"console"} type={"bool"} value={"False"} />
<PyParameter name={"service_name"} type={"str"} value={"'liman'"} />

</div>

<PyFunctionReturn type={"LimanInstrumentor"} />

</PyFunction>

</Tab>

</Tabs>
