---
title: ServiceAccountSpec
---

## Attributes

<PyAttribute name={"kind"} type={"str"} value={"'ServiceAccount'"} />

<PyAttribute name={"context"} type={"Context | None"} value={"None"} />

<PyAttribute name={"credentials_provider"} type={"str | None"} value={"None"} />

<PyAttribute
  name={"credentials_providers"}
  type={"list[str] | None"}
  value={"None"}
/>

## Functions

<PyFunction name={"validate_credentials_exclusive"} type={"validate_credentials_exclusive(cls, /, data) -> Any"}>

<PySourceCode >

```python
@model_validator(mode="before")
@classmethod
def validate_credentials_exclusive(cls, data: Any) -> Any:
    if data.get("credentials_provider") and data.get("credentials_providers"):
        raise InvalidSpecError(
            "Cannot specify both credentials_provider and credentials_providers"
        )
    return data
```

</PySourceCode>

<div >

<PyParameter name={"cls"} type={null} value={null} />
<PyParameter name={"data"} type={"Any"} value={null} />

</div>

<PyFunctionReturn type={"Any"} />

</PyFunction>

<PyFunction name={"validate_required_fields"} type={"validate_required_fields(self) -> ServiceAccountSpec"}>

<PySourceCode >

```python
@model_validator(mode="after")
def validate_required_fields(self) -> ServiceAccountSpec:
    has_credentials = bool(self.credentials_provider or self.credentials_providers)
    has_context = bool(self.context)

    if not has_credentials and not has_context:
        raise InvalidSpecError(
            "ServiceAccount must have either credentials_provider/credentials_providers or context"
        )

    return self
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"ServiceAccountSpec"} />

</PyFunction>
